import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  container: {
    flex: 3,
    alignItems: 'center',
  },
  logo: {
    width: 230,
    height: 230,
  },
});

export default style;
